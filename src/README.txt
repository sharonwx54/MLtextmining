SUMMARY
================================================================================
The python environment I use is Python 3.10.6 under PyCharm C/E ARM, with macOS Monterey (M2 Chip). The requirement packages are listed in requirement.txt

To execute in command line, simply call ./hw4.sh train_file test_file. Note the two file name should has the format "../data/XXXX.scale.trn.libsvm" or "../data/XXXX.scale.tst.libsvm"